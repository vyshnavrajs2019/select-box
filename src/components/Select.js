import React from 'react';

import classes from './Select.module.css';

function Select({ search=true, mulitselect=false, options=[] }) {
	return (
		<div className={classes.select}>
			
		</div>
	)
}

export default Select;